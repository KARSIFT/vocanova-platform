{
  "name": "@vocanova/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc --watch --noEmit --pretty",
    "build": "tsc -b --pretty false",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit --pretty false"
  }
}
