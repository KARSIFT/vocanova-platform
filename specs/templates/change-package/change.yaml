schema_version: 1

id: VOC-000
slug: replace-with-approved-slug
title: REPLACE WITH APPROVED TITLE

type: placeholder
status: draft
risk: R0
canonical_path: specs/changes/VOC-000-replace-with-approved-slug

approval_status: not-approved
implementation_authorized: false
# Per AGENTS.md "Drafting automatic_merge_allowed": set per declared risk class
# before the plan PR is reviewed — do not leave this as an unexamined inherit.
# R0–R2: true unless the package records a specific reason to require founder
# eyes on the merge into develop. R3: true or false with stated justification
# (comment here or adjacent one-line note). R4: false while the pre-VOC-079
# authority remains active. Deliberate false on R0–R2 must state why. The field
# is a package policy record; no current workflow consumes it or performs merge.
# It never substitutes for risk classification, CI, independent verification,
# complete R4 evidence, action-specific authority, or EHR.
automatic_merge_allowed: true
production_impact: unknown
rollback_required: true
