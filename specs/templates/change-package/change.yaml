schema_version: 1

id: VOC-000
slug: replace-with-approved-slug
title: REPLACE WITH APPROVED TITLE

type: placeholder
status: draft
risk: R0
canonical_path: specs/changes/VOC-000-replace-with-approved-slug

approval_status: not-approved
implementation_authorized: false
# Planned implementation shape defaults to one coherent PR. If this package needs
# more than one, replace the rationale with the concrete boundary, partial-state,
# integration, rollback, and overhead explanation.
implementation_pull_request_count: 1
multiple_pull_request_rationale: not-applicable-single-implementation-pr-default
# Per AGENTS.md "Drafting automatic_merge_allowed": examine this field before
# plan review. R0–R4 all default to true; a risk label alone is never an opt-out.
# To use false, add an adjacent top-level automatic_merge_hold_reason with the
# specific package-local rationale (uncomment and replace the example below).
# The field is a package policy record. Governance consumes it only for the read-only
# eligibility report; no current workflow uses it to perform a merge.
# It never substitutes for risk classification, CI, independent verification,
# complete R4 evidence, action-specific authority, or EHR.
automatic_merge_allowed: true
# automatic_merge_hold_reason: REPLACE WITH SPECIFIC PACKAGE-LOCAL HOLD RATIONALE
production_impact: unknown
rollback_required: true
