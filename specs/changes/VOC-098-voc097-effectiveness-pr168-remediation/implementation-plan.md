# VOC-098 — Implementation Plan

## Preconditions

1. Independently review and adopt this exact package; complete truthful adoption
   bookkeeping, exact bookkeeping review, genuine eligibility, normal non-author plan
   merge, and applicable post-merge checks.
2. Fetch current `main`/`develop`, inspect open PRs/issues/worktrees/refs, and confirm
   PR #168 remains preserved at its expected rejected head with no post-review edits or
   external action.
3. Re-read PR #167 lifecycle evidence and all three PR #168 FAIL reviews. Never treat
   a prior PASS/FAIL, issue, plan merge, or package field as external-action authority.

## Ordered repository work

1. Integrate the adopted VOC-098 ancestry into the same PR #168 branch without
   discarding, rewriting, or force-pushing the rejected implementation history. Stop
   on unexpected overlap or drift.
2. Add exactly the nine VOC-097 package surfaces to the authorized union. Reconcile
   their lifecycle/effectiveness/task claims to the exact PR #167 facts in `D02`,
   preserve candidate/adoption history, and keep all ACT-03/04/05 and production holds.
3. In the existing workflow/policy/test files, implement an integrity-bound non-secret
   deadline handoff and atomically compare the first secret-bearing step start to both
   effective deadlines before its first secret reference. Test strict before/equal/
   after boundaries and an injected inter-step delay.
4. In the existing public GitHub runtime client/tests, enforce a 5-second connection
   deadline and a 15-second whole-response deadline through body completion. Add
   deterministic delayed-connect and stalled-body tests without network retries.
5. In the existing strict JSON/JCS implementation/tests, reject lone surrogates in
   object keys and string values recursively and retain valid pair behavior.
6. Reconcile exactly the four known operative VOC-094 27-file claims to 29-core/
   38-total. Strengthen the final-evidence test so it scans both VOC-094 and VOC-096
   and cannot pass merely because corrected text exists elsewhere. Preserve historical
   27-path evidence explicitly as history.
7. Verify the exact authorized/actual path inventories: 47 authorized; 46 expected
   diffs; only byte-identical `apps/web/worker-configuration.d.ts` absent from the
   diff after locked regeneration. Run the complete test plan, secret scan, production
   sentinel/hash comparison, and no-external-effect audit.
8. Commit/push a fresh corrected PR #168 head. Update the PR evidence without deleting
   old FAILs. Obtain three fresh different-actor exact-SHA PASS reviews. Any material
   edit requires checks and all applicable reviews again.
9. A distinct non-author actor audits genuine eligibility and merges normally. Record
   exact source head, merge SHA, hosted/post-merge checks, source-head readback and
   recreation command, and preserved worktree/ref inventory.

## Stop conditions

Stop on a 48th required path, incomplete PR #167 evidence, inability to enforce both
deadlines before secret read, timeout/body leakage, invalid-Unicode ambiguity, a stale
operative scope survivor, production/hold drift, secret-like content, failed mandatory
check, or any need for live-system action. Do not weaken or bypass a gate.
