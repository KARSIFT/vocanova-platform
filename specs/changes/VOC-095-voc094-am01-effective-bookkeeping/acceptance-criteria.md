# VOC-095 — Acceptance criteria

## VOC-095-AC-00 — Complete bounded package

The plan contains exactly one complete VOC-095 package, one task, one implementation
PR, R4 classification, explicit `automatic_merge_allowed: true`, rollback, owners,
protected areas, evidence, and no external-action authority. The plan PR changes no
VOC-094 or living document.

## VOC-095-AC-01 — Exact lifecycle evidence

The implementation PR binds final candidate `aad884a6d53c5e0f13b94f8042774b14a07015af`,
independent final review, Governance run `32913984893` with `eligible: true` and
`reasons: []`, merge `75e5c9909fe105a9af3e6e8a3600fec27fcbd593`, lifecycle evidence,
and successful post-merge runs `32914336969`, `32914336980`, and `32914336981`.

## VOC-095-AC-02 — Truthful effectiveness

VOC-094 no longer states that AM-01 bookkeeping, PR-160 merge, or applicable
post-merge checks are pending. Its AM-01 gate, implementation authority, adoption,
release, and blocker claims agree with the bound evidence. Historic facts remain
preserved, and ACT-02/ACT-03/ACT-04/ACT-05 remain separately held.

## VOC-095-AC-03 — Incident preservation

The ACT-01 sequencing incident and D1 UUID
`22ae386f-e3f5-4d98-a3ad-18b39d3b8556` are recorded as non-secret evidence. The
correction performs no D1, Cloudflare, DNS, deployment, migration, data, billing,
production, or launch action.

## VOC-095-AC-04 — Fresh action boundary

The corrected record explicitly requires a fresh exact corrected-SHA ACT-02 review,
overlay/resource review, Free/$0 evidence, and time-bounded action authority before
any live command. No VOC-095 field implies that the preserved D1 is usable.

## VOC-095-AC-05 — Validation and lifecycle controls

Governance validation, risk classification, `git diff --check`, applicable hosted
checks, exact independent R4 review, non-author merge, post-merge readback, and
recreation/lifecycle evidence all pass. Rollback is a reviewed revert of the
repository-only correction and does not mutate external systems.
