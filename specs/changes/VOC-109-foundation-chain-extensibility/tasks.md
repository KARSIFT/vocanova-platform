# VOC-109 — Tasks

## VOC-109-T00 — Restore the guarded foundation extension point

- Requirement source: `VOC-109-D00` through `VOC-109-D07`
- Acceptance criteria: `VOC-109-AC-00` through `VOC-109-AC-05`
- Tests: `VOC-109-TEST-00` through `VOC-109-TEST-05`
- Evidence: `VOC-109-EV-00` through `VOC-109-EV-05`
- Implementation pull-request mapping: one coherent implementation PR
- Risk: R3
- Status: pending review and adoption

Change only the active VOC-081 validator and focused test so the exact original
eight-command prefix and terminal test remain unique, direct, ordered, and non-
bypassable while declared canonical lowercase-single-hyphen checks with distinct
single direct Node entry points can occupy the one guarded extension slot. Run all
required evidence, rollback, specialist, and independent review steps. Prove the
exact positive fixture pairing package script `ci:f3-evidence` with value
`node scripts/foundation/voc105-f3-evidence-policy.mjs` and the sole extension segment
`pnpm run ci:f3-evidence`, without requiring those independently canonical identifiers
to be identical. The accountable VOC-109 owner also records the bounded first real
VOC-105 integration observation and applies its stop/remediation disposition. Do not
add the VOC-105 command to repository `package.json` or perform any external action.

One task is the minimum-sufficient unit because the parser and its positive/negative
regression proof form one protected replacement boundary. Splitting them would expose
an unproved validator state and duplicate branch, exact-SHA review, hosted-check,
coordination, elapsed-time, and bookkeeping overhead.
