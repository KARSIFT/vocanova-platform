# VOC-012 Tasks

## VOC-012-T01 — Verify authority and base

Confirm founder direction `5052251828`, base `f1596ba9...`, instructions, active A-003,
PR #41 state, and a clean tree.

## VOC-012-T02 — Prepare package-only candidate

Add exactly nine VOC-012 files and one index entry without editing VOC-011, VOC-010,
VOC-006, or any excluded path.

## VOC-012-T03 — Reproduce PR #41 failure

Verify exact base/candidate/merge/tree/timestamp/checks and zero comments/reviews;
record that chat evidence and content equivalence cannot cure the gate.

## VOC-012-T04 — Derive and execute governed revert

After package sync, mechanically reverse PR #41's exact ten paths on a fresh branch.

## VOC-012-T05 — Enforce scope and activation invariants

Prove zero excluded changes and all six activation/automation values remain disabled.

## VOC-012-T06 — Prepare fresh VOC-011 adoption

After revert merge, atomically restore the approved VOC-011 package/index in a new
candidate from then-current `develop`.

## VOC-012-T07 — Gate fresh evidence before merge

Obtain a new exact-SHA independent report posted on the fresh PR before human merge;
reuse no PR #41 report, review, check, or tree-equivalence evidence as approval.

## VOC-012-T08 — Synchronize remediation lifecycle

Record all exact package/revert/fresh-adoption evidence and enable only resumption of
the PR #40 remediation while keeping issue #39 open.

## VOC-012-T09 — Validate every exact candidate

Run governance, YAML/link, risk, diff, scope, hosted, and reverse-tree checks; publish
draft PRs and stop without self-approval or merge.

## Traceability

Tasks `T01`–`T09` map to `AC-01`–`AC-12`. This package PR performs only `T01`–`T03`
and the package portion of `T09`.
