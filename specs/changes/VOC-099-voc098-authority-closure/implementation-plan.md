# VOC-099 — Implementation Plan

## Plan adoption preconditions

1. Independently review the exact VOC-099 candidate. The plan author cannot review or
   adopt it and no prior VOC-098 review transfers.
2. Before plan merge, update bookkeeping to the exact approved candidate/review and
   adoption evidence, `status: adopted`, and `implementation.authorized: true`.
   Remove draft/pending implementation language. Do not add
   `authority_effective: false` or a future self-bookkeeping condition.
3. Obtain a fresh different-actor exact-SHA bookkeeping review, genuine eligibility,
   and normal non-author plan merge. Record post-merge checks as evidence. Once the
   adopted plan is on `develop`, its declared repository-only authority is usable.

## Ordered repository work

1. Fetch current `main`/`develop`, inspect open PRs/issues/worktrees/refs, and confirm
   PR #168 is still preserved at its expected rejected head. Integrate the adopted
   VOC-099 ancestry into the same branch without reset, history rewrite, force-push,
   deletion, or loss of uncommitted work; stop on overlap or drift.
2. Add exactly the nine VOC-098 package surfaces to the authorized union. Reconcile
   all direct lifecycle, effectiveness, task, readiness, closure, and blocker claims
   to PR #170 facts. Preserve its approved candidate/adoption history and every
   external hold; eliminate recursive false/draft/future-repair language.
3. Retain and complete VOC-098's nine-file VOC-097 reconciliation and four existing
   PR #168 remediations: atomic first-secret deadline enforcement; distinct connect
   and whole-response timeouts; recursive lone-surrogate validation; and exact
   package-scope text/test closure.
4. Verify exact inventories: 56 authorized paths, 55 expected diffs, and only
   `apps/web/worker-configuration.d.ts` absent after locked regeneration. Run the
   complete test plan, secret scan, production sentinel/hash comparison, and
   no-external-effect audit.
5. Commit/push a fresh PR #168 head without deleting old FAIL evidence. Obtain three
   fresh different-actor exact-SHA PASS reviews. Any material edit repeats checks and
   all applicable reviews.
6. A distinct non-author actor audits genuine eligibility and merges normally.
   Record exact source head, merge SHA, hosted/post-merge checks, source-head readback
   and recreation command, and preserved worktree/ref inventory.

## Stop conditions

Stop on a 57th required path, a missing PR #170 fact, any recursive effectiveness
condition, incomplete VOC-098 remediation, production/hold drift, secret-like content,
failed mandatory check, or any need for live-system action. Do not weaken or bypass a
gate.
