# VOC-099 — Acceptance Criteria

## VOC-099-AC-00 — Exact governed shape

The plan changes only its nine package files and defines one task. After adoption is
merged to `develop`, the same PR #168 resumes with exactly 56 authorized paths and 55
expected actual diffs; the sole expected non-diff is the recorded byte-identical web
generated type. No 57th path, replacement PR, worktree/ref deletion, or external
action occurs.

## VOC-099-AC-01 — Recursive authority gate is removed

Final pre-merge bookkeeping records candidate `10a9a822...`, all three independent
PASS reviews, `VOC-099-ADOPT-01`, `status: adopted`, and
`implementation.authorized: true`. The adopted package contains no false authority-
effectiveness field, adopted-but-inactive implementation/task claim, future self-
bookkeeping dependency, or requirement for another plan to activate its repository
authority. Fresh bookkeeping review, eligibility, non-author merge, and post-merge
checks remain intact.

## VOC-099-AC-02 — VOC-098 lifecycle is truthful

All nine VOC-098 surfaces consistently bind the exact PR #170 review, literal
`eligible: true` / `reasons: []`, normal merge, successful post-merge checks, and
lifecycle readback. Repository authority is usable and task status truthful; no text
claims those completed events remain pending. Every external action stays held.

## VOC-099-AC-03 — VOC-098 corrections remain complete

All VOC-097 lifecycle reconciliation and four rejected-PR remediations authorized by
VOC-098 remain in the fresh candidate and pass their exact negative/positive tests.
No prepared-binder, expiry, public-read, Unicode/JCS, resource, cost, secret,
production, rollback, or scope gate is weakened.

## VOC-099-AC-04 — Fresh exact revision is independently accepted

Rejected SHA `cde0f665...` and its three FAILs remain immutable and unapproved. A
fresh corrected SHA passes complete local/hosted validation and separate Cloudflare,
security/settings, and exact R4 reviews; a different non-author merges only after
genuine eligibility. All external and production holds remain unchanged.
