# VOC-011 Tasks

## VOC-011-T01 — Verify authority and base

Confirm founder direction `5047420157`, base `8de351bd...`, instructions, active
A-003, and a clean tree.

## VOC-011-T02 — Prepare package-only candidate

Add exactly nine VOC-011 files and the index entry without editing VOC-010 or any
excluded path.

## VOC-011-T03 — Reproduce PR #40 failure

Verify exact base/candidate/merge/tree/timestamp/checks and zero GitHub comments or
reviews; record that external evidence cannot be reused.

## VOC-011-T04 — Derive and execute governed revert

After package sync, mechanically reverse PR #40's exact ten paths on a fresh branch.

## VOC-011-T05 — Enforce scope and activation invariants

Prove zero excluded changes and all six activation values remain disabled.

## VOC-011-T06 — Prepare fresh VOC-010 adoption

After revert merge, atomically restore the approved VOC-010 package/index in a new
candidate from then-current `develop`.

## VOC-011-T07 — Gate fresh evidence before merge

Obtain a new exact-SHA independent report recorded on the fresh PR before human merge;
reuse no PR #40 evidence.

## VOC-011-T08 — Synchronize remediation lifecycle

Record all exact package/revert/fresh-adoption evidence and enable only the later
VOC-006 correction stage while keeping issue #39 open.

## VOC-011-T09 — Validate every exact candidate

Run governance, YAML/link, risk, diff, scope, hosted, and reverse-tree checks; publish
draft PRs and stop without self-approval or merge.

## Traceability

Tasks `T01`–`T09` map to `AC-01`–`AC-12`. This package PR performs only `T01`–`T03`
and the package portion of `T09`.
