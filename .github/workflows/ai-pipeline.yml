name: pipeline

# Thin file. All real logic lives in KARSIFT/karsift-ai-infra's reusable
# workflows - this just wires triggers to them. Runs alongside (not instead
# of) this repo's own governance-policy.yml / repository-governance.yml.
#
# There is deliberately no "plan" job here yet: turning a founder request into
# an approved VOC-### change package is still a human+chat-session process
# (DOC-18 Phase 3, Change Contract automation, isn't built). What's automated
# starts one step later: an already-adopted package's task gets implemented,
# checked, and independently reviewed - a human still opens the implementation
# issue and still triggers `implement` by hand via workflow_dispatch.
#
# auto_merge_enabled stays "false" here: this repo's own governance
# (docs/governance/a003-transition-state.yaml) shows automatic_merge_allowed
# as false today - governance permission and technical activation are
# separate states, and this file must not represent a capability as active
# just because policy permits it eventually. Flip it only alongside a real
# activation record in that governance, not as an isolated workflow edit.

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      change_id:
        description: 'Adopted package to implement from, e.g. "VOC-007"'
        required: true
        type: string
      package_path:
        description: 'Path under specs/changes/, e.g. "specs/changes/VOC-007-some-slug"'
        required: true
        type: string
      task_id:
        description: "Task ID from the package's tasks.md"
        required: true
        type: string
      issue_number:
        description: "GitHub issue carrying implementation authority"
        required: true
        type: string
      attempt:
        description: "1 or 2"
        required: false
        type: string
        default: "1"

jobs:
  implement:
    if: github.event_name == 'workflow_dispatch'
    uses: KARSIFT/karsift-ai-infra/.github/workflows/implement.yml@main
    with:
      change_id: ${{ inputs.change_id }}
      package_path: ${{ inputs.package_path }}
      task_id: ${{ inputs.task_id }}
      issue_number: ${{ inputs.issue_number }}
      attempt: ${{ inputs.attempt }}
    secrets: inherit

  ci:
    if: github.event_name == 'pull_request'
    uses: KARSIFT/karsift-ai-infra/.github/workflows/ci.yml@main

  extract-package-path:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      package_path: ${{ steps.extract.outputs.package_path }}
    steps:
      - name: Parse package path from PR body
        id: extract
        run: |
          path=$(echo "${{ github.event.pull_request.body }}" | grep -oP '(?<=Package path: `)[^`]+')
          if [ -z "$path" ]; then
            echo "No 'Package path: \`...\`' line found in the PR body - review cannot run without it." >&2
            exit 1
          fi
          echo "package_path=$path" >> "$GITHUB_OUTPUT"

  review:
    needs: [ci, extract-package-path]
    if: github.event_name == 'pull_request'
    uses: KARSIFT/karsift-ai-infra/.github/workflows/review.yml@main
    with:
      pr_number: ${{ github.event.pull_request.number }}
      package_path: ${{ needs.extract-package-path.outputs.package_path }}
    secrets: inherit

  merge-gate:
    if: github.event_name == 'issue_comment' || github.event_name == 'pull_request'
    uses: KARSIFT/karsift-ai-infra/.github/workflows/merge-gate.yml@main
    with:
      founder_username: "m-e-h-r-d-a-a-d"
      auto_merge_enabled: "false"
    secrets: inherit
